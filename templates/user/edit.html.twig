{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Edit Article</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_index', {'id': app.user.id}) }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
